# Source Notes and Provenance

Generated on 2026-07-14.

## Inputs Successfully Used

- `C:\Users\johar\OneDrive\Documents\Joshua  Harris - Resume - Current.docx`
  - Extracted text directly from `word/document.xml`.
  - Used as the baseline for career chronology, employers, titles, dates, awards, and legacy project details.

- `E:\gitrepos\AxMule`
  - Reviewed selected canonical architecture, capability, Cassandra audit, migration, README, and resume/runbook documents.
  - Used to shape modern positioning around autonomous systems, DevOps, cloud migration, observability, data platforms, GPU compute, governance, and AI agents.

## Inputs Attempted With Limitations

- `https://chatgpt.com/share/6a568a5d-8e98-83e8-abb2-cfe681c8a3cc`
  - Browser fetch did not return usable shared-chat content in this environment.
  - No claims were based on unavailable chat content.

- `https://www.linkedin.com/in/chaossyn/`
  - Browser/search access did not return a usable public profile body in this environment.
  - The LinkedIn URL is included as a profile link only. No claims were derived from inaccessible LinkedIn content.

## GitHub Push Status

Target remote requested:

`git@github.com:ISBC8SYN/coresyn.git`

GitHub host verification was fixed by appending GitHub's official SSH host key entries to `C:\Users\johar\.ssh\known_hosts` from GitHub Docs. Clone and push were still blocked because GitHub rejected the available local SSH key:

`git@github.com: Permission denied (publickey).`

The local repository was staged under:

`E:\gitrepos\AxMule\coresyn`

Remote is intended to be configured as:

`origin git@github.com:ISBC8SYN/coresyn.git`

Once a GitHub-authorized SSH key or credential is available, push can be completed with:

```powershell
cd E:\gitrepos\AxMule\coresyn
git push -u origin main
```

## External Host-Key Reference

GitHub official SSH host key fingerprints and known_hosts entries were checked against:

https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/githubs-ssh-key-fingerprints
